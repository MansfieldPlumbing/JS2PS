# Fixtures

This directory contains only inputs exercised by a current measurement or
benchmark. The OGL files are pinned to an immutable npm package and checked
against `ogl/manifest.json` by the offline verification suite.

Third-party fixtures must include accurate provenance, declared licensing, and
content hashes before being committed. Inputs that are not wired to a concrete
gate do not belong here.
